# Database Configuration
DATABASE_URL=mysql://root:YOUR_PASSWORD@localhost:3306/financial_data_scraper

# JWT Secret (generate using: openssl rand -base64 32)
JWT_SECRET=your_generated_secret_string_here

# Perplexity API (Optional but recommended for smart fallback)
PERPLEXITY_API_KEY=pplx-xxxxxxxxxxxxxxxxxxxxxxxx

# AWS S3 Configuration (if using S3 storage)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=
AWS_S3_BUCKET=

# Server Configuration
PORT=5000
NODE_ENV=development

# CORS Configuration (add your frontend URL)
CORS_ORIGIN=http://localhost:3000
